name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - '**/*.ts'
      - '**/*.tsx'
      - '**/*.js'
      - '**/*.mjs'
      - '**/*.cjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      # Workflow files affect what runs in CI and the supply-chain surface
      # we expose, so scan them too. Also satisfies the branch ruleset's
      # `code_scanning` requirement on PRs that only touch workflows.
      - '.github/workflows/**.yml'
      - '.github/workflows/**.yaml'
  schedule:
    - cron: "37 5 * * 1"

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript"]
    steps:
      - uses: actions/checkout@v6
      - uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          queries: security-extended,security-and-quality
      - uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
